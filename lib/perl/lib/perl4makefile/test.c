#include <stdlib.h>
#include <stdio.h>
/*fprintf ( stderr, "this in an experimet [%d]",aa->c->d  [ a+ yy  [ a[k]] ][xxx[c][d]]) = cedric[c->d[a]][n];*/

main ()
{
  int a, b;

  for ( a=0; a<10; a++)
    fprintf ( stderr, "UN");fprintf ( stderr, "DEUX");
  assert (0);
}
      

